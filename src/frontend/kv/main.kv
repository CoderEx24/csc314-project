<AppScreen>:
    anchor_x: 'center'
    anchor_y: 'center'

    size_hint: (1, 1)

    ScreenManager:
        size_hint: (1, 1)
        id: main_manager

        LoginScreen:
            name: "login"

        SignupScreen:
            name: "signup"
